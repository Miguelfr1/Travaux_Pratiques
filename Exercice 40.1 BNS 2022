def recherche(elt, tab) :
    liste = []

    for i in range(len(tab)) :
        if  elt == tab[i] :
            liste.append((i,tab[i],))
            return liste
        else :
            return liste


    





recherche(3, [3, 2, 1, 3, 2, 1])
recherche(4, [1, 2, 3])
